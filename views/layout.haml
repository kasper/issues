!!!
%html
  %head
    %title= yield_content :title
    = stylesheet_link_tag "#{timestamped_filename('./public/style.css')}"
  %body
  
    #wrapper
    
      #user-nav
        %ul
          - if !authorised?
            %li= link_to('Sign in' , '/signin')
            %li= link_to('Sign up' , '/signup')
          - else
            %li= authorised_user.username
            %li= link_to('Logout' , '/logout') 
        
      #global-nav
        %ul
          %li= link_to('Issues' , '/')
          %li= link_to('New issue' , '/issues/new')
          - if authorised?
            %li= link_to('My issues' , '/')
          %li= link_to('Tags' , '/tags')
        
      #content= yield